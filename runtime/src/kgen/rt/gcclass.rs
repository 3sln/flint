// Generated by kin from the source named below. Do not edit.
// source: flint.rt.gcclass

#![allow(unused_imports)]
use crate::gc::InternTable;
use crate::hash;
use crate::eq::*;
use crate::map::*;
use crate::mem::Addr;
use crate::mem::Space;
use crate::gc::Gc;
use crate::obj::*;
use crate::rt::Rt;
use crate::seqs::*;
use crate::strs::INTERN_MAX;
use crate::vector::*;
use crate::value::{Value, FALSE, NIL, NOT_FOUND, TRUE};

/// The free list a hole of `size` bytes belongs on.
/// 
/// Answers `NCLASS - 1` for anything at or above `NCLASS * 8`, which is
/// the list holding every hole too big to have one of its own.
pub fn gc_class_of(size: Addr) -> i64 {
    if (((size as u64) >> 3) as i64) < (crate::gc::NCLASS as i64) {
        return ((size as u64) >> 3) as i64;
    }
    return (crate::gc::NCLASS as i64) - 1;
}
