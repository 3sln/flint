// Generated by kin from the source named below. Do not edit.
// source: flint.rt.numkind

#![allow(unused_imports)]
use crate::gc::InternTable;
use crate::hash;
use crate::eq::*;
use crate::map::*;
use crate::mem::Addr;
use crate::obj::*;
use crate::rt::Rt;
use crate::seqs::*;
use crate::strs::INTERN_MAX;
use crate::vector::*;
use crate::value::{Value, FALSE, NIL, NOT_FOUND, TRUE};
use crate::kgen::rt::hash::*;
use crate::kgen::rt::pike::*;

impl Rt {
    /// Is `v` an INTEGER -- a fixnum or a bigint?
    /// 
    /// The fixnum test comes first because it is a test on the value word and
    /// the other needs a tag read, and because almost every integer is one.
    pub fn is_int(&self, v: Value) -> bool {
        if v.is_fixnum() {
            return true;
        }
        if !v.is_heap() {
            return false;
        }
        return ty(&self.gc.sp, v.as_heap()) == TY_BIGINT;
    }
    /// Is `v` a NUMBER -- either integer tier, or a double?
    /// 
    /// The double first, for the same reason: it is the value-word test.
    pub fn is_number(&self, v: Value) -> bool {
        return v.is_double() || self.is_int(v);
    }
}
