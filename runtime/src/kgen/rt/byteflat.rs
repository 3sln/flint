// Generated by kin from the source named below. Do not edit.
// source: flint.rt.byteflat

#![allow(unused_imports)]
use crate::gc::InternTable;
use crate::hash;
use crate::eq::*;
use crate::map::*;
use crate::mem::Addr;
use crate::obj::*;
use crate::rt::Rt;
use crate::seqs::*;
use crate::strs::INTERN_MAX;
use crate::vector::*;
use crate::value::{Value, FALSE, NIL, NOT_FOUND, TRUE};
use crate::kgen::rt::hash::*;
use crate::kgen::rt::pike::*;

impl Rt {
    /// Append every byte of `v` to the sink `s`.
    /// 
    /// Answers nothing: the bytes are the answer, and they go where the caller
    /// said. A value that is not a byte string appends nothing rather than
    /// refusing -- every caller here has already decided what `v` is.
    pub fn b_append(&mut self, v: Value, s: u32) {
        if !v.is_heap() {
            return;
        }
        let t: u8 = ty(&self.gc.sp, v.as_heap());
        if t == TY_BYTES {
            self.sink_put_run(s, v.as_heap() + crate::obj::HDR, self.olen(v));
            return;
        }
        if t != TY_BROPE {
            return;
        }
        // ALREADY FLATTENED: one run rather than a descent.
        let flat: Value = self.slot(v, crate::bytes::BB_FLAT);
        if !flat.is_nil() {
            self.sink_put_run(s, flat.as_heap() + crate::obj::HDR, self.olen(flat));
            return;
        }
        let n: u32 = self.olen(v) - crate::bytes::BB_KIDS;
        for i in 0..n {
            self.b_append(self.slot(v, crate::bytes::BB_KIDS + i), s);
        }
    }
    /// `v` as one flat byte string, whatever tier it was.
    /// 
    /// The sink is opened and closed HERE, which is the shape every user of one
    /// should have: whoever opens it closes it, and nothing else can be holding
    /// an index into it by the time it goes.
    pub fn b_to_flat(&mut self, v: Value) -> Value {
        let s: u32 = self.sink_open();
        self.b_append(v, s);
        let out: Value = self.sink_bytes(s);
        self.sink_close(s);
        return out;
    }
}
