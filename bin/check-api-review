#!/usr/bin/env bash
# Every published surface has a section in `doc/api-review.md`: what a guest can
# name, what an embedder calls, what a user types. See the docstring in
# check_api_review.py, and `AGENTS.md` §9 for the rule it enforces.
exec python3 "$(dirname "$0")/check_api_review.py" "$@"
